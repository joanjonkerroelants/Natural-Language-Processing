# Natural-Language-Processing
assigment 1, 2 and 3 for NLP 

it can and should all be done via the terminal 

1: git clone "url"
2: uv sync 
3: source .venv/bin/activate
3.5: python main.py -h (if you want to see all the hyperparameters and what they do)
4: python main.py {model} {type} {chancing the parameters}